# Temporizador con Gemini Studio

Este proyecto implementa un temporizador que, al finalizar, envía un mensaje a la API de Gemini Studio.

## Instalación

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt